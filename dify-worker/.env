# --- Required services ---
DATABASE_URL=postgresql://dify:dify@localhost:5432/dify
QDRANT_URL=http://host.docker.internal:6334
SANDBOX_SERVICE_URL=http://host.docker.internal:8194
CELERY_BROKER_URL=redis://host.docker.internal:6379/0

# --- Health server ---
HEALTH_PORT=8080

# --- Force all writes under /temp ---
TMPDIR=/temp
STORAGE_LOCAL_PATH=/temp/storage

# --- OpenDAL local FS (newer Dify default path) ---
OPENDAL_SCHEME=fs
OPENDAL_FS_ROOT=/temp/storage

# Optional tuning
# CELERY_WORKER_AMOUNT=1
# CELERY_LOG_LEVEL=INFO
