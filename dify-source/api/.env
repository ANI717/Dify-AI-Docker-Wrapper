# Role
# MODE=api
# DIFY_BIND_ADDRESS=0.0.0.0
DIFY_PORT=8080

# Force temp writes to /temp
TMPDIR=/temp

# DB (pgvector) & Redis reachable on host; adjust as needed
DB_USERNAME=dify
DB_PASSWORD=dify
DB_HOST=host.docker.internal
DB_PORT=5432
DB_DATABASE=dify

# REDIS_HOST=host.docker.internal
# REDIS_PORT=6379
# REDIS_DB=0
CELERY_BROKER_URL=redis://:@host.docker.internal:6379/1

# Vector store
VECTOR_STORE=pgvector

STORAGE_TYPE=local
STORAGE_LOCAL_PATH=/temp
# ---- Azure Blob storage (officially supported method) ----
# STORAGE_TYPE=azure-blob
# AZURE_BLOB_ACCOUNT_NAME=your-account
# AZURE_BLOB_ACCOUNT_KEY=your-account-key
# AZURE_BLOB_CONTAINER_NAME=your-container
# AZURE_BLOB_ACCOUNT_URL=https://your-account.blob.core.windows.net
# ---------------------------------------------------------

# Optional provider keys
# OPENAI_API_KEY=...
# ANTHROPIC_API_KEY=...

# Sandbox execution service reachable on host; adjust as needed
CODE_EXECUTION_ENDPOINT=http://host.docker.internal:8194
CODE_EXECUTION_API_KEY=dify-sandbox

# exact UI origins to allow (comma-separated)
CONSOLE_CORS_ALLOW_ORIGINS=http://localhost:8080,http://127.0.0.1:8080
WEB_API_CORS_ALLOW_ORIGINS=http://localhost:8080,http://127.0.0.1:8080

# keep these aligned for URL building
CONSOLE_WEB_URL=http://localhost:8080
CONSOLE_API_URL=http://127.0.0.1:5001

# Setup for plugin development; adjust as needed
PLUGIN_HOST=http://host.docker.internal:5005
