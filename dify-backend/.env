# API will listen here (supervisord leaves it to the api process)
DIFY_BIND_ADDRESS=0.0.0.0
DIFY_PORT=5001

# Temp writes (you said only /temp is writable)
TMPDIR=/temp

# Database (pgvector) — adjust host/creds
DB_USERNAME=dify
DB_PASSWORD=strongpass
DB_HOST=host.docker.internal
DB_PORT=5432
DB_DATABASE=dify

# Redis / Celery
REDIS_HOST=host.docker.internal
REDIS_PORT=6379
REDIS_DB=0
CELERY_BROKER_URL=redis://:@host.docker.internal:6379/1

# Vector store
VECTOR_STORE=pgvector

# Storage
STORAGE_TYPE=local
STORAGE_LOCAL_PATH=/temp

# (Optional) model/provider keys
# OPENAI_API_KEY=...
# ANTHROPIC_API_KEY=...

# --- CORS / URLs (so your browser can call the API) ---
CONSOLE_CORS_ALLOW_ORIGINS=http://localhost:8080
WEB_API_CORS_ALLOW_ORIGINS=http://localhost:8080
CONSOLE_WEB_URL=http://localhost:8080
SERVICE_API_URL=http://127.0.0.1:5001
