# Port we expose on the host
REDIS_PORT=6379

# Strongly recommended
REDIS_PASSWORD=change-me

# Persistence (recommended for durability)
REDIS_AOF=yes         # append-only log
# Optional RDB snapshot schedule, e.g. "900 1 300 10 60 10000" or leave empty
REDIS_SAVE=
